/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["picsum.photos", "ui-avatars.com", "i.pinimg.com","res.cloudinary.com", "randomuser.me", "via.placeholder.com"], // Add both domains here
  },
};

export default nextConfig;
